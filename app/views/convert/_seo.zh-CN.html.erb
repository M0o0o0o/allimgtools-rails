<section class="border-t border-base-200">
  <div class="max-w-3xl mx-auto px-6 sm:px-10 py-20 space-y-20">

    <%# ── Hero ──────────────────────────────────────────────────────── %>
    <div class="space-y-5">
      <h1 class="text-4xl sm:text-5xl font-extrabold tracking-tight text-base-content leading-tight">
        在线转换图片 —<br class="hidden sm:block"> 免费格式转换器
      </h1>
      <p class="text-base-content/55 leading-relaxed text-base sm:text-lg max-w-2xl">
        立即在线转换您的图片格式 — 无需软件，无需注册。我们的免费在线图片转换器支持 JPEG、PNG、WebP、GIF 和 AVIF 格式。切换到 WebP 以获得更快的网站速度，将 PNG 转换为 JPEG 以减小文件大小，或将图片准备成平台所需的精确格式。上传您的图片，并在几秒钟内下载转换后的文件。
      </p>
      <div class="rounded-2xl overflow-hidden border border-base-200 shadow-sm mt-4">
        <%= image_tag "/images/seo/convert_hero.png", alt: "在线图片格式转换器 — 立即转换 JPEG、PNG、WebP、GIF 和 AVIF", class: "w-full h-auto object-cover", loading: "lazy", width: "800", height: "450" %>
      </div>
    </div>

    <%# ── How to ─────────────────────────────────────────────────────── %>
    <div>
      <div class="mb-8">
        <span class="text-xs font-bold uppercase tracking-widest text-primary">分步指南</span>
        <h2 class="text-2xl sm:text-3xl font-bold text-base-content mt-2">如何在线转换图片</h2>
      </div>
      <ol class="space-y-5">
        <li class="flex gap-5 items-start">
          <span class="flex h-9 w-9 shrink-0 items-center justify-center rounded-xl bg-primary/10 text-primary font-extrabold text-sm mt-0.5">1</span>
          <div class="pt-1.5">
            <p class="font-semibold text-base-content mb-1">上传您的图片</p>
            <p class="text-sm text-base-content/55 leading-relaxed">拖放或点击从您的设备中选择一个 JPEG、PNG、WebP、GIF 或 AVIF 文件。</p>
          </div>
        </li>
        <li class="flex gap-5 items-start">
          <span class="flex h-9 w-9 shrink-0 items-center justify-center rounded-xl bg-primary/10 text-primary font-extrabold text-sm mt-0.5">2</span>
          <div class="pt-1.5">
            <p class="font-semibold text-base-content mb-1">选择输出格式</p>
            <p class="text-sm text-base-content/55 leading-relaxed">选择您的目标格式 — JPEG、PNG、WebP、GIF 或 AVIF。转换器会自动处理所有技术细节。</p>
          </div>
        </li>
        <li class="flex gap-5 items-start">
          <span class="flex h-9 w-9 shrink-0 items-center justify-center rounded-xl bg-primary/10 text-primary font-extrabold text-sm mt-0.5">3</span>
          <div class="pt-1.5">
            <p class="font-semibold text-base-content mb-1">下载您转换后的图片</p>
            <p class="text-sm text-base-content/55 leading-relaxed">点击下载并立即保存您转换后的文件，随时随地使用。</p>
          </div>
        </li>
      </ol>
      <p class="text-xs text-base-content/35 mt-6 flex items-center gap-2">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/>
        </svg>
        无需账户。您的文件绝不会存储在我们的服务器上。
      </p>
    </div>

    <%# ── Format comparison table ────────────────────────────────────── %>
    <div>
      <div class="mb-8">
        <span class="text-xs font-bold uppercase tracking-widest text-primary">参考</span>
        <h2 class="text-2xl sm:text-3xl font-bold text-base-content mt-2">JPEG、PNG、WebP、GIF 和 AVIF — 哪种格式适合您？</h2>
      </div>
      <p class="text-base-content/55 mb-6 text-sm leading-relaxed">
        每种图片格式都有其独特的优势。使用此表格选择适合您需求的输出格式：
      </p>
      <div class="overflow-x-auto rounded-2xl border border-base-200">
        <table class="table w-full">
          <thead>
            <tr class="bg-base-200/60 border-base-200">
              <th class="text-xs text-base-content/50 font-semibold uppercase tracking-wide">格式</th>
              <th class="text-xs text-base-content/50 font-semibold uppercase tracking-wide">最佳用途</th>
            </tr>
          </thead>
          <tbody>
            <% [
              ["JPEG",  "照片、网页图片、社交媒体 — 兼容性强，自然场景文件小"],
              ["PNG",   "标志、插图、截图 — 无损质量，支持完全透明"],
              ["WebP",  "现代网络 — 最佳整体压缩；在相同质量下比 JPEG 更小，支持透明"],
              ["GIF",   "简单动画、颜色有限的图形 — 动画内容平台支持广泛"],
              ["AVIF",  "下一代网络 — 最高的压缩效率和卓越的质量；是性能关键型网站的理想选择"],
            ].each do |fmt, desc| %>
              <tr class="border-base-200">
                <td class="text-sm font-medium text-base-content"><%= fmt %></td>
                <td class="text-sm text-base-content/60"><%= desc %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
      <p class="text-xs text-base-content/35 mt-3">如果不确定，用于网页时转换为 WebP，需要透明度和无损质量时转换为 PNG。</p>
    </div>

    <%# ── Use cases ──────────────────────────────────────────────────── %>
    <div>
      <div class="mb-8">
        <span class="text-xs font-bold uppercase tracking-widest text-primary">用例</span>
        <h2 class="text-2xl sm:text-3xl font-bold text-base-content mt-2">为什么要转换图片格式？</h2>
      </div>
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
        <% [
          ["转换为 WebP 用于网页",           "在同等质量下，WebP 文件比 JPEG 小 25-35%。将您网站的图片切换到 WebP 是提高页面速度和核心网页指标得分最快的方法之一。"],
          ["PNG 转 JPEG 以减小文件",         "PNG 文件是无损的且文件较大。如果您的图片不需要透明度，转换为 JPEG 或 WebP 可以显著减小文件大小 — 通常可减少 60-80%。"],
          ["JPEG 转 PNG 以获得质量和透明度", "需要添加透明背景或处理需要像素级精度的图片？转换为 PNG 可获得无损质量和 Alpha 通道支持。"],
          ["平台要求的格式",             "不同的工具、CMS 平台和设计工作流程需要特定的文件格式。在这里转换一次，每次都上传正确的格式。"],
        ].each do |title, desc| %>
          <div class="rounded-2xl border border-base-200 bg-base-100 p-6 hover:border-primary/20 hover:shadow-sm transition-all">
            <p class="font-semibold text-base-content mb-2"><%= title.html_safe %></p>
            <p class="text-sm text-base-content/55 leading-relaxed"><%= desc %></p>
          </div>
        <% end %>
      </div>
    </div>

    <%# ── Web performance section ────────────────────────────────────── %>
    <div>
      <div class="mb-8">
        <span class="text-xs font-bold uppercase tracking-widest text-primary">网页性能</span>
        <h2 class="text-2xl sm:text-3xl font-bold text-base-content mt-2">将图片转换为 WebP 以加快网站速度</h2>
      </div>
      <p class="text-base-content/55 mb-6 text-sm leading-relaxed">
        Google 的 PageSpeed Insights 和 Lighthouse 审计经常将“以下一代格式提供图片”标记为一项关键优化。在上传前将图片转换为 WebP 或 AVIF 有助于您：
      </p>
      <ul class="space-y-4 mb-8">
        <% [
          ["改善核心网页指标",        "更小的图片文件可减少 LCP（最大内容绘制），这是 Google 的主要排名信号之一。"],
          ["降低带宽和托管成本", "现代格式以更小的文件大小提供相同的视觉质量，从而减少服务器负载和数据传输。"],
          ["通过 Lighthouse 审计",          "将传统的 JPEG 和 PNG 文件转换为 WebP 或 AVIF 直接解决了 PageSpeed Insights 最常见的警告之一。"],
        ].each do |title, desc| %>
          <li class="flex gap-4 items-start">
            <span class="flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-primary/15 mt-0.5">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3 text-primary" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M5 13l4 4L19 7"/>
              </svg>
            </span>
            <p class="text-sm text-base-content/70"><strong class="text-base-content font-semibold"><%= title %></strong> — <%= desc %></p>
          </li>
        <% end %>
      </ul>
      <div class="bg-base-200/50 rounded-xl px-5 py-4 text-sm text-base-content/55 leading-relaxed border border-base-200">
        <strong class="text-base-content font-semibold">WordPress 提示：</strong> 在上传到媒体库之前，请在此处将您的图片转换为 WebP。这可以避免插件依赖，并让您完全控制输出质量和格式。
      </div>
    </div>

    <%# ── Bulk conversion ───────────────────────────────────────────── %>
    <div>
      <div class="mb-8">
        <span class="text-xs font-bold uppercase tracking-widest text-primary">专业功能</span>
        <h2 class="text-2xl sm:text-3xl font-bold text-base-content mt-2">批量图片转换</h2>
      </div>
      <p class="text-base-content/55 mb-6 text-sm leading-relaxed">
        需要一次转换多张图片？我们的批量转换工具让您可以一次性上传和处理多个文件 — 非常适合：
      </p>
      <ul class="space-y-3 mb-8">
        <% [
          ["电子商务",    "在发布到您的商店之前，将整个产品图片库转换为 WebP。"],
          ["博客和内容", "一次性将特色图片和内联照片批量转换为一致的格式。"],
          ["设计导出", "将包含 PNG 设计资产的文件夹转换为 JPEG 或 WebP，用于交付或网页发布。"],
        ].each do |label, desc| %>
          <li class="flex gap-3 items-start">
            <span class="flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-base-200 mt-0.5">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3 text-base-content/40" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M5 13l4 4L19 7"/>
              </svg>
            </span>
            <p class="text-sm text-base-content/70"><strong class="text-base-content font-semibold"><%= label.html_safe %></strong> — <%= desc %></p>
          </li>
        <% end %>
      </ul>
      <div class="relative overflow-hidden rounded-2xl bg-gradient-to-br from-primary to-primary/70 p-6 flex items-center justify-between gap-6">
        <div class="absolute -right-6 -top-6 h-24 w-24 rounded-full bg-white/10 pointer-events-none"></div>
        <div class="absolute right-8 bottom-0 h-14 w-14 rounded-full bg-white/5 pointer-events-none"></div>
        <div class="relative">
          <p class="font-bold text-white mb-0.5">批量转换在专业版中可用</p>
          <p class="text-white/60 text-sm">一键将多张图片转换为任意格式。</p>
        </div>
        <%= link_to pricing_path, class: "relative shrink-0 btn btn-sm bg-white text-primary hover:bg-white/90 border-0 font-semibold shadow-sm" do %>
          免费开始 →
        <% end %>
      </div>
    </div>

    <%# ── FAQ ───────────────────────────────────────────────────────── %>
    <div>
      <div class="mb-8">
        <span class="text-xs font-bold uppercase tracking-widest text-primary">常见问题</span>
        <h2 class="text-2xl sm:text-3xl font-bold text-base-content mt-2">常见问题</h2>
      </div>
      <div class="space-y-2">
        <% [
          ["有损和无损转换有什么区别？",
           "有损转换（例如，将 PNG 转换为 JPEG 或 WebP）通过永久删除部分数据来压缩图片，从而生成文件更小但质量略有折衷的图片。无损转换（例如，JPEG 转换为 PNG）保留所有像素数据，但可能不会减小文件大小。对于用于网络的照片，WebP 等有损格式提供了最佳的尺寸与质量平衡。"],
          ["WebP 是否所有浏览器都支持？",
           "是的。WebP 已被所有主要的现代浏览器完全支持 — Chrome、Firefox、Safari、Edge 和 Opera — 覆盖了全球 97% 以上的网页用户。它现在是网页图片的推荐格式，对于大多数项目来说，可以安全使用而无需备用方案。"],
          ["将 PNG 转换为 JPEG 时，透明度会发生什么变化？",
           "JPEG 不支持透明度。当您将带有透明背景的 PNG 转换为 JPEG 时，透明区域会被纯色（通常是白色）填充。要保留透明度，请转换为 WebP 或 PNG — 这两种格式都支持 Alpha 通道。"],
          ["哪种图片格式最适合网站？",
           "WebP 是 2025 年大多数网站的最佳综合格式。它在同等质量下比 JPEG 文件小 25-35%，支持透明度，并受所有现代浏览器支持。对于尖端浏览器上的最大压缩，AVIF 甚至更进一步 — 但 WebP 在兼容性和性能之间取得了最佳平衡。"],
          ["图片格式之间有什么区别？",
           "JPEG 使用有损压缩，非常适合照片。PNG 使用无损压缩并支持透明度，非常适合图形和标志。WebP 结合了两者的优点 — 有损或无损，文件更小并支持透明度。GIF 支持简单动画，但仅限于 256 种颜色。AVIF 是最新的格式，具有最佳压缩效果，尽管浏览器采用率仍在增长。"],
          ["我可以一次批量转换多张图片吗？",
           "批量转换在专业版套餐中可用。您可以一次上传多张图片，并通过一次操作将它们全部转换为您选择的格式 — 在处理大量图片集时节省大量时间。"],
        ].each do |question, answer| %>
          <div class="collapse collapse-plus rounded-xl border border-base-200 bg-base-100 hover:border-primary/20 transition-colors">
            <input type="checkbox">
            <div class="collapse-title font-semibold text-base-content text-sm"><%= question %></div>
            <div class="collapse-content">
              <p class="text-sm text-base-content/60 leading-relaxed"><%= answer %></p>
            </div>
          </div>
        <% end %>
      </div>
    </div>

    <%# ── Footer note ─────────────────────────────────────────────────── %>
    <p class="text-xs text-base-content/25 text-right">最后更新：2026 年 5 月</p>

  </div>
</section>
